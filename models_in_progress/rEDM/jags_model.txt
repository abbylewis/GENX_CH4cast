model
    {

    #Priors
    tau_proc ~ dgamma(0.001, 0.001) #process error
    
    #Model
    
    for (i in 1:n){
		mu_proc[i] ~ dnorm(edm_mu[i], edm_tau[i])
		y[i] ~ dnorm(mu_proc[i], tau_proc)
	}
    
    }